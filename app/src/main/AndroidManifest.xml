<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.darkfan.repo.simple">

    <application
        android:name=".app.SimpleApp"
        android:allowBackup="true"
        android:icon="@mipmap/dfkernel_ic_launcher"
        android:label="@string/dfkernel_app_name"
        android:roundIcon="@mipmap/dfkernel_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DfkernelAppTheme">

        <activity android:name=".test.TestActivity"
            android:exported="true">

            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>
</manifest>